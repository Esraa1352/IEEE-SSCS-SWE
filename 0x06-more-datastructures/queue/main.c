#include"queue.h"

int main()
{
 int data[max_size];
 _enqueue(data,5);
 _enqueue(data,3);
 _enqueue(data,7);
 _dequeue(data);
 _front(data);
 _isEmpty (data);
 _size(data);
 _clear(data);
 return 0;
}
