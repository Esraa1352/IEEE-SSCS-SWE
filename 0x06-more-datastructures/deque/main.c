#include"deque.h"

int main()
{
int data[size];
 push_front(data,5);
 push_front(data,6);
 push_back ( data,7);
 push_back ( data,8);
 pop_front(data);
 pop_back (data);
 _front( data);
 back(data);
 _size(data);
 clear(data);
}