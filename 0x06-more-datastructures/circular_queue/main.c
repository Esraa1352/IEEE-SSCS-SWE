#include "circular_queue.h"

int main()
{
    int data [size];
    _enque(data,1);
    _enque(data,2);
    _enque(data,3);
    _enque(data,3);
    _enque(data,3);
    _enque(data,3);
    _deque(data);
    _enque(data,5);
    
}